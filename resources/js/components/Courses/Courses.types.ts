import { Course } from '@/types';

export interface TypeColors {
    codeBg: string;
    codeText: string;
    border: string;
    hover: string;
    /** Section header styling (icon, badge, border) */
    accentBg?: string;
    accentText?: string;
    accentBorder?: string;
    badgeBg?: string;
    badgeText?: string;
}

